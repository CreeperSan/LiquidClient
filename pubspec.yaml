name: liquid_client
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

# 需要支持多平台（Android, iOS, MacOS, Windows）
dependencies:
  flutter:
    sdk: flutter
  oktoast: ^2.3.2
  provider: ^4.3.2+2
  cupertino_icons: ^1.0.0
  shared_preferences: ^0.5.12+4
  dio: 3.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

